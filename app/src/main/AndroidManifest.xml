<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rothaugealex.de.tv_fernbedienung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Einstellungen" />
        <activity android:name=".Favorites" />
        <activity android:name=".Sender" />
        <activity android:name=".Benachrichtigung" />
        <activity android:name=".Informationen" />
        <activity android:name=".Reset" />
        <activity android:name=".Sprache" />
        <activity android:name=".AutomatischesAbschalten"></activity>
    </application>

</manifest>